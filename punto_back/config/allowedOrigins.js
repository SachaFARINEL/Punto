// Define a list of allowed origins for CORS
const allowedOrigins = [
    'http://localhost:3000', // Add the localhost domain and port as an allowed origin
]

// Export the allowed origins list for use in other modules
module.exports = allowedOrigins
